$(function() {
    $('.list').css('font-size', '20px')

    $('.list, #h1FF, #pFF').css('background', 'yellow')

    // $('.highlight').css('background', 'yellow')

    // let contents = $('#pFF').html();
    // alert(contents);

    // alert( 'The DOM has finished loading!' );
});